Alpha 13 implementation note: the closed-loop behavior is intentionally minimal. It adds no cloud dependency and preserves the existing Alpha 12 navigation. Acceptance is determined by CI unit tests plus APK build output.
