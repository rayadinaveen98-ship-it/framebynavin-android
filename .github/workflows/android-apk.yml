name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      - "feature/v1.3.2-reliability-recovery"
      - "repair/v1.3.2-release-blockers"
      - "feature/v142-framebynavin-v4-schedule"
      - "fix/v143-profile-weekly-progress"
      - "feature/v144-guide-schedule-refresh"
      - "fix/v144-post-qa-guide-revenue"
      - "feature/v146-guide-stability-ux"
    paths:
      - "app/**"
      - "*.gradle.kts"
      - "gradle.properties"
      - ".github/workflows/android-apk.yml"

concurrency:
  group: android-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Run unit tests
        run: gradle :app:testDebugUnitTest --stacktrace

      - name: Run Android lint
        shell: bash
        run: |
          set +e
          gradle :app:lintDebug --stacktrace
          status=$?
          if [ "$status" -ne 0 ]; then
            report="app/build/intermediates/lint_intermediate_text_report/debug/lintReportDebug/lint-results-debug.txt"
            if [ -f "$report" ]; then
              echo "----- FULL LINT REPORT -----"
              cat "$report"
              echo "----- END LINT REPORT -----"
            fi
            exit "$status"
          fi

      - name: Compile instrumentation test APK
        run: gradle :app:assembleDebugAndroidTest --stacktrace

      - name: Run release-blocker UI smoke tests
        if: github.ref == 'refs/heads/repair/v1.3.2-release-blockers'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: gradle :app:connectedDebugAndroidTest --stacktrace

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Backlot-debug-${{ github.run_number }}
          path: app/build/outputs/apk/debug/app-debug.apk

  ui-smoke-v144:
    if: github.ref == 'refs/heads/feature/v144-guide-schedule-refresh' || github.ref == 'refs/heads/fix/v144-post-qa-guide-revenue'
    runs-on: macos-15-intel
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Run V144 connected UI smoke tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: gradle :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.framebynavin.app.ui.V132ReleaseBlockerUiTest --stacktrace

  ui-smoke-v146:
    if: github.ref == 'refs/heads/feature/v146-guide-stability-ux'
    runs-on: macos-15-intel
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Run V146 guide and deep-link regression smoke
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          arch: x86_64
          profile: pixel_6
          disable-animations: true
          script: gradle :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.framebynavin.app.ui.V146GuideAssetDecodeTest,com.framebynavin.app.reminders.V146ProjectPulseDeepLinkTest --stacktrace
