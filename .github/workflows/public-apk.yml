name: Build FrameByNavin Public APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "app/**"
      - "tools/v175_integrate.py"
      - "tools/v175_public_prepare.py"
      - ".github/workflows/public-apk.yml"
      - "*.gradle.kts"
      - "gradle.properties"

concurrency:
  group: framebynavin-public-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    if: ${{ github.event.repository.private == false }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Apply v1.7.5 integration
        run: python3 tools/v175_integrate.py

      - name: Apply public-safe preparation
        run: python3 tools/v175_public_prepare.py

      - name: Public-source safety gate
        shell: bash
        run: |
          set -euo pipefail
          test ! -e keystore/REMOVED_PRIVATE_SIGNING_VALUE.jks
          ! grep -R --exclude-dir=.git --exclude='*.lock' -n 'REMOVED_PRIVATE_SIGNING_VALUE' .
          grep -q 'versionCode = 37' app/build.gradle.kts
          grep -q 'versionName = "1.7.5-original-frames-splash-rc1"' app/build.gradle.kts
          grep -q 'PickMultipleVisualMedia' app/src/main/java/com/framebynavin/app/ui/V175BestFramesUi.kt
          grep -q 'input.copyTo(output' app/src/main/java/com/framebynavin/app/ui/V175BestFramesUi.kt

      - name: Unit tests
        run: gradle :app:testDebugUnitTest --stacktrace

      - name: Android lint
        run: gradle :app:lintDebug --stacktrace

      - name: Compile instrumentation tests
        run: gradle :app:assembleDebugAndroidTest --stacktrace

      - name: Build APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          test -s app/build/outputs/apk/debug/app-debug.apk
          sha256sum app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FrameByNavin-v1.7.5-public-debug
          path: app/build/outputs/apk/debug/app-debug.apk
