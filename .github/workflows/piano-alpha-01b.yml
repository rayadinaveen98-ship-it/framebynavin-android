name: Piano Studio Alpha 0.1B APK

on:
  push:
    branches:
      - "ci/piano-studio-alpha-01b-20260906"
    paths:
      - "piano-ci/**"
      - ".github/workflows/piano-alpha-01b.yml"
  workflow_dispatch:

concurrency:
  group: piano-alpha-01b-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android 36 native toolchain
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager "platforms;android-36" "build-tools;36.0.0" "ndk;28.2.13676358" "cmake;3.22.1"

      - name: Pin Android 36-compatible UI stack
        shell: bash
        run: |
          grep -RIl 'compileSdk = 37' piano-ci --include='*.gradle.kts' | xargs -r sed -i 's/compileSdk = 37/compileSdk = 36/g'
          grep -RIl 'compose-bom:2026.08.00' piano-ci --include='*.gradle.kts' | xargs -r sed -i 's/compose-bom:2026.08.00/compose-bom:2026.04.01/g'
          grep -RIl 'activity-compose:1.13.0' piano-ci --include='*.gradle.kts' | xargs -r sed -i 's/activity-compose:1.13.0/activity-compose:1.11.0/g'
          grep -RIl 'navigation-compose:2.10.0' piano-ci --include='*.gradle.kts' | xargs -r sed -i 's/navigation-compose:2.10.0/navigation-compose:2.9.6/g'
          grep -RIl 'lifecycle-runtime-compose:2.11.0' piano-ci --include='*.gradle.kts' | xargs -r sed -i 's/lifecycle-runtime-compose:2.11.0/lifecycle-runtime-compose:2.9.4/g'
          grep -R "compileSdk =\|compose-bom:\|activity-compose:\|navigation-compose:\|lifecycle-runtime-compose:" piano-ci --include='*.gradle.kts'

      - name: Set up Gradle 9.6
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.6.0"

      - name: Build debug APK
        working-directory: piano-ci
        run: gradle :app:assembleDebug --stacktrace

      - name: Verify native engine is packaged
        shell: bash
        run: |
          set -euo pipefail
          apk="piano-ci/app/build/outputs/apk/debug/app-debug.apk"
          test -s "$apk"
          unzip -l "$apk" | grep -q "lib/arm64-v8a/libpianostudio_audio.so"
          mkdir -p piano-ci/dist
          cp "$apk" piano-ci/dist/PianoStudio-alpha-0.1B-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PianoStudio-alpha-0.1B
          path: piano-ci/dist/PianoStudio-alpha-0.1B-debug.apk
